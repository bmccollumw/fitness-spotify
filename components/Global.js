
import React from 'react';
import { startAuthorization } from '../utils/authUtils';

const Global = () => {
  return (
    <div>
      <h1>meow on global</h1>
    </div>
  );
};

export default Global;